<%= render 'admin/sidebar' %>
<div class="flash-box">
	<%= render 'layouts/flash', flash: flash %>
</div>
<div id="canvas">
	<% @stores.each do |store| %>
		<div class="shadow-box item">
			<img />
			<h4 class="item-name"><%= slice_name(store.name, 17) %></h4>
			<h5 class="item-description"><%= "Today's Sale: #{current_user.company.currency}"%></h5>
			<%= link_to '<i class="icon-pencil"></i>'.html_safe, edit_admin_store_path(store), class: "edit", title: "Edit" %>
			<%= link_to '<i class="icon-trash"></i>'.html_safe, admin_store_path(store), class: "trash", title: "Delete", method: :delete, confirm: "Are you sure?" %>
		</div>
	<% end %>
</div>
<div class="span2">
	<a href="<%= new_admin_store_path %>" id="add-item" title="Add New Store">
		<div class="shadow-box item-small">
			<%= image_tag "icons/glyphicons_190_circle_plus.png" %><br/>
			New Store
		</div>
	</a>
</div>